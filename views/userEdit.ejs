

<style>
  form{
    text-align: center;

    margin-top: 4rem;

    height: auto;

    padding: 4rem;

  }
</style>
<html lang="en" data-darkreader-mode="dynamic" data-darkreader-scheme="dark">
<link rel="stylesheet" href="/login&signup.css">

<div class="sticky-div bg-white justify-content-center">

  <div class="m-lg-auto">
          <form action="/admin/edit-user" method="post">
            <% if(user) { %>
              <%= console.log('=== inside', user) %>
    
            <div class="form-group p-5">
              <div class="input-group p-5">
                <div class="input-group-prepend">
                </div>
                <input type="text" id="user" class="form-control" name="name" placeholder="Username"  value="<%= user.name%>"
                  autocomplete="off" required="required">
              </div>
            </div>
            <div class="form-group">
              <div class="input-group">
                <input type="email" id="pass" class="form-control" name="email" placeholder="Password" value="<%= user.email%>"
                  required="required">
              </div>
            </div>
            <div class="form-group">
              <div class="input-group">
                <div class="form">
                  <button type="submit" class="btn btn-primary btn-lg">Edit User</a></button>
                </div>
                <%  }else{ %>
                  <tr>
                    <td colspan="3">No user</td>
                  </tr>
                  <% } %>
          </form>
        </div>
  </div>